import { asyncHendler } from "../utils/AsyncHendler.util.js";
import { ApiError } from "../utils/ApiError.util.js";
import { ApiResponse } from "../utils/ApiResponse.util.js";

const getPlatlist = asyncHendler((req,rres)=>{

})

const addVideoPlaylist = asyncHendler((req,res)=>{

})

const removeVideo = asyncHendler((req,res)=>{

})

export{
    getPlatlist,addVideoPlaylist,removeVideo
}